{
  "name": "games",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/fire.ts --bundle --minify --sourcemap --outfile=dist/js/fire.min.js && cp src/fire.html dist/index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "esbuild": "^0.25.2",
    "tone": "^15.0.4"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
